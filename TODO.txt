WHAT WAS JUST ACCOMPLISHED

WHERE TO START NOW
* FIX search page bookmarks sometimes re-uses listing tab instead of search tab
* MOD Allow editing of listing details and be smart about which fields to overwrite when scraping/refreshing
	* Do not overwrite when refreshing
		* Address fields
		* misc listing data? i.e. sqft entered manually, prevent overwriting with null
	* Add/Edit delete history items
	* Display %s with listing price changes


STUFF TO DO LATER
* use a DB instead of key-value store
* listing sets
	* personal data applies within a set
	* think realtor managing clients
	* associate searches with a listing set
	* ability to run all searches for a listing set
	* ability to run all searches for all listing sets
* convert from event page back to background page for live updates to listings
	* OR have an event listener for local save, replace listings live!
* architecture
	* restructure directories by functional area?
* search page
	* more search fields
* scraping
	* capture more data
* listings page
	* floating left filter bar
	* floating top bar
	* results paging, beginning to be a memory/cpu hog
	* each listing
		* render picture list dynamically only when thumbnail clicked
		* turn personal notes textarea into plain div, but editable textarea once clicked
		* collapsible history
	* filters/sort
		* status dropdown filter